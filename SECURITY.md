# 安全策略

## 支持的版本

| 版本 | 支持 |
| --- | --- |
| `main` 分支 | ✅ |
| 最近一个 release tag | ✅ |
| 更早版本 | ❌ |

## 报告漏洞

请**不要**在公开 Issue 中报告安全漏洞。使用 GitHub 的 [私密漏洞报告](../../security/advisories/new)（Security → Advisories → New），或在与维护者建立私密通道后披露。

报告请包含：问题描述、复现步骤、影响评估（尤其是否破坏以下任一信任边界）、建议修复。

## 响应目标

- 72 小时内确认接收；
- 7 日内给出初步评估与处置计划；
- 修复后公开致谢（如你愿意）。

## 范围与诚实声明

Marshal 的信任模型针对**本地单用户 Profile**：它降低误操作与流程风险，**不宣称抵抗同 UID 恶意进程或恶意代码沙箱逃逸**。以下情形不属于本模型漏洞（但欢迎作为加固建议提出）：

- 宿主机上同 UID 进程读取 `.marshal/` 或凭据目录；
- Worker 进程在宿主机上的任意非授权副作用（这是 Local Profile 的已知边界，见 `docs/security-model.md`）。

属于漏洞的典型情形：证据门禁可被 Worker 绕过（如伪造 Verification/Review 绑定）、凭据隔离失效（Worker 获得 Publisher 凭据）、生命周期守卫可被陈旧写入突破、发布幂等性破坏导致重复/越权发布。
