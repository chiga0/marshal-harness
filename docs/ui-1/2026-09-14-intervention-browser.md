# 2026-09-14 intervention 界面真实浏览器增量验收

## 范围与身份

本次仅验证既有失败任务的异常说明、诊断导航及相关布局。没有创建 Task、启动模型、重试、取消或修改业务数据；服务由维护者持有，验收者只连接和读取。本文不关闭 UI-1，也不构成正式发行包或完整产品验收。

- 实际服务：`http://127.0.0.1:54390/ui/`。
- 实际任务：`task-fbe8e6bb-83be-4768-abe9-c0d05f383ff7`，`intervention`，revision 15，`previous_execution_unresolved`；存在 `unknown` 作者，待答/待批准为 0。
- UI 为源码构建，包含异常提示修改 `a9a3fb50` 及问答边界修正 `c92527cb`，不是已签公开 RC2 原包。
- 浏览器：已安装 Chromium `149.0.7827.55`，Playwright 驱动独立 headless 实例，没有接管维护者的浏览器会话，没有下载新浏览器。
- 凭据仅从维护者提供的精确连接文件读取并填入同源登录表单；不写入 URL，不记录或截取凭据输入。

实际 HTTP 下载的 UI 资源身份如下，均返回 200：

| 资源 | 字节数 | SHA-256 |
| --- | ---: | --- |
| `index-Drh9IYGW.js` | 538760 | `282ca9cd63a47c2d0198ab638e4291b30122777dcf3396b7b53c1297e9ef3e90` |
| `index-C9k2p4Hr.css` | 28247 | `3b9f246ceedebb343e9f35761fed33987b1fe42bf969b0677f6f1eee98f29f74` |

## 三条验收线

| 验收线 | 结论 | 实测范围与缺口 |
| --- | --- | --- |
| 功能与可靠性 | PARTIAL | 真实服务读取、异常与零待办一致显示、团队/活动/受影响 Worker 路由通过。未执行恢复、业务交付或本批完整自动化矩阵。 |
| 视觉与交互 | PARTIAL | 桌面、375px 窄屏、CSS 200% 放大截图已目视检查；键盘 Enter 导航与 Escape 关闭详情通过。未测原生浏览器 200% 缩放、Safari 和完整主题矩阵。 |
| 产品可用性 | NOT_RUN | 没有真实目标用户无指导测试。Agent 自动化走查不能替代；验收执行 Agent 曾参与这项 UI 修改，本文不冒称与实现作者独立的人类体验证据，仍由主侧复核。 |

### 已通过的增量检查

1. 概览顶部存在 `role=alert` 的“系统执行异常，需要排查”，明确它不等同于待答问题。
2. 原因展示真实 `previous_execution_unresolved`，并说明当前界面没有安全恢复操作；没有伪造重试/恢复按钮，没有建议强制清理或重复建任务。
3. “需要你的处理（0 项）”同时说明系统异常未解决，不再将无待办暗示为运行正常。
4. 对“查看团队与 Worker”执行键盘聚焦和 Enter，进入实际团队页；点击活动入口进入活动页；点击受影响 Worker 进入对应详情。
5. Worker 详情打开后，Escape 正常关闭，之后概览可达。首次脚本未关闭详情就点击被遮挡的背景概览而超时；复测明确关闭详情后通过，保留原脚本失败记录，不将其误报为产品缺陷。
6. 375px 视口文档宽度为 375px；1024px 视口应用 CSS `zoom: 2` 后文档宽度仍为 1024px，未出现整页横向溢出。CSS 放大不是原生浏览器缩放，不能替代后者验收。

## 本机证据与适用边界

证据根目录：`/Users/gawain/Documents/codebase/github/marshal-harness/.marshal/ui-intervention-browser-20260914/`。

- `desktop.png`、`narrow.png`、`zoom-200.png`、`activity.png`：真实页面截图。
- `result.json`：首次导航检查及背景点击超时原记录。
- `layout-result.json`：Escape 关闭、窄屏与 CSS 放大复测。
- `assets.json`：实际服务静态资源字节与摘要。

这些文件属于本机私有验收证据，未提交运行数据；远端读者不能假定本机路径可访问。当前记录证明既有干预状态有明确说明与可达诊断入口，不证明干预状态已经恢复，也不证明真实业务任务成功。后续正式 UI 发布仍须补齐原生缩放与真实用户可用性等适用缺口。
