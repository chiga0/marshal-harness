# 执行透明度、工作台与从零验收

本批目标：用户不需要读取 hash 或私有数据库，就能理解任务阶段、成员工作、阻塞、策略与成果；交付前由研发方消化可复现的功能与交互缺陷。基线 `80f6fed5203b8b6c52c3add0d3852a7ca512b6cf`。本文先冻结验收，结果另行追加，不以案例存在表示通过。

## 产品出口

1. 工作台：哪些任务需要我、哪些正在执行、哪些已有成果。计数注明加载范围。
2. 任务工作台：目标、当前阶段、当前焦点、阻塞及下一步；阶段取自真实状态与执行事实，不制造进度百分比。
3. 成员详情：工作目标、Agent/模型来源、执行耗时、实际输入、公开活动/工具记录、输出与用量。技术身份默认折叠。
4. 策略与成果：展示原 Leader 决定、独立 Review 意见及验收依据的可读正文，绑定原证据；不通过另一个模型猜测原因。
5. 运行中静默不能称为思考；只在 Provider 明确报告时显示相应活动标签，不保存隐藏推理正文。未报告模型/Token/费用明确显示缺失与覆盖。
6. 实际输入只表示 Marshal 交给 Agent 的内容，不能声称包含 Agent 自有系统提示、Skill 或完整模型上下文。披露为明确受信策略，有界并标注脱敏/截断；不恢复任意披露回调。

## 冻结业务案例

所有正常案例使用无敏感合成资料、同一通用文件配置。输入与预期在任务开始前冻结；不修改需求迎合输出。真实模型失误与人为故障分别记录。每次记录候选、安装包/页面摘要、任务 ID、耗时、干预、模型调用、重试/返工及失败原因；用量缺失不填零。

| ID | 难度与任务 | 独立完成断言 |
| --- | --- | --- |
| S01 | 简单说明文档：蓝杉读书会，每周六14:00，地点城市图书馆二层；标题、三条参与步骤、两条注意事项 | 从连接、页面创建、批准到下载；事实逐项准确，标题与步骤完整，没有新增矛盾；真实 Review/Verification/最终交付均完成 |
| S02 | 单文件活动邀请 HTML：蓝杉读书会、2026-10-17 14:00、城市图书馆二层；介绍/日程/报名三个区块，锚点报名按钮，不依赖外网 | 独立消费下载内容；DOM事实/区块完整，按钮到达报名区，桌面与375px无横滚、无外部请求；在禁脚本上下文消费静态HTML并做视觉检查，不冒充JS运行验证 |
| M01 | 合成订单报告：east paid 1200、east paid 0、west paid 800、west refunded -200、east cancelled 999；用户明确口径为paid及refunded计入、cancelled排除 | 精确期望east 2笔/1200分、west 2笔/600分、总4笔/1800分；由独立算式核对；附件与结果可追溯 |
| M02 | 两份规格冲突：上线日期与范围不同；要求先问用户再输出决定及迁移清单 | 页面展示冲突问题，按预定答案“10月20日上线，仅只读查询”答复；结果不包含写入上线，覆盖原迁移条目；刷新不重复创建 |
| C01 | 三份资料合成实施方案：需求、约束、风险，互补分析后整合 | 同一计划有真实互补分工与整合依赖；完整覆盖离线可用、仅本地存储、禁止外部发布、验收/回退四项要求；交付可消费；记录已准入执行从started到settlement的区间交叠（包含cleanup），不单独证明模型计算同时发生 |
| C02 | 真实内容拒收后局部修正 | 仅自然出现独立返工时计真实覆盖；原意见进入修正输入，无关成果保持，原预算不重置。未自然发生记未覆盖，并另跑受控反例，不能诱导作者故意犯错 |

## 控制与异常案例

| ID | 操作 | 断言 |
| --- | --- | --- |
| F01 | 运行中取消 | 原操作收口，所属执行清理，无替身或虚假交付；页面说明实际停止状态 |
| F02 | 正常停开、刷新、丢失一次操作响应 | 同根读取原事实，原键重放同回执，不自动重新创建/批准；令牌不进入URL、存储或截图 |
| F03 | 超限输入、外部发布或执行SQL请求 | 可理解地拒绝或说明不支持，不虚构外部成功，不无限等待 |
| F04 | 错ticket、迟到、重复、取消后活动、旧配置 | 不接纳错误权威结果；活动不伪装成当前执行；原配置不被静默迁移 |
| F05 | 工具范围、用量、输入披露反例 | 注册与实际工具事件分别检查；未知工具不放宽；累计用量不重复计；敏感字段不无差别公开；prepared不冒充handed-off |

## 可理解性与视觉检查

所有S/M/C案例都通过页面检查：当前阶段、谁在执行/等谁、最近策略、问题原因、下一步、最终成果位置。成员输入必须是实际记录，不用计划目标冒充。终态停止运行动画；陈旧观察注明时间。

浅深主题覆盖1440/1024/375px及200%缩放；检查运行、待答、失败、交付、长中文、长ID、无用量。整页不横滚，主操作可达；键盘可开关成员详情、操作确认及返回；刷新不抢焦点、不把读旧记录的滚动位置拉到底。截图来自实际实现，受控状态与真实模型分别标注。

## 执行与收敛

三个完整研发包并行：可靠交付接缝、执行透明度数据链、任务工作台。共享合同先审，独立worktree/单写者，集中集成后独立审查，不按字段拆PR。

先明确构建UI，再从冻结候选打包/核验；新settings、安装目录、私有数据根与独立端口，不改用户现有launcher、设置或运行服务。模型登录沿原Agent使用，不复制凭据。浏览器创建/回答/批准/下载，API仅作只读断言；受控故障单独允许测试拦截，记录真实服务响应。

先跑确定性回归，再S→M→C真实模型；失败定位后修复并复测影响范围，不盲目重跑。最终候选S01连续3次、M01连续2次、C01一次，S02/M02及控制异常各有对应证据。所有失败留在分母。该门槛是有限样本验收，不能承诺绝对没有未知缺陷。

功能可靠性、视觉交互和Agent模拟走查由研发方完成，不把简单问题交给用户反复发现。按既有三线规则，真人可用性只能由未参与开发的目标用户/代表完成；未执行如实保留，不冒充PASS，也不阻止继续完成全部可自动与模拟验证的工作。

## 参考

- 本地Multica：`packages/views/common/task-transcript`、`packages/views/layout/animated-right-sidebar.tsx`，参考执行记录和渐进详情，保留Marshal任务模型。
- [Langfuse观察分类](https://langfuse.com/docs/observability/features/observation-types)与[记录层级](https://langfuse.com/docs/observability/best-practices)：参考Agent、模型调用与工具活动的关系，不强制引入外部追踪服务。
- [shadcn侧栏布局](https://ui.shadcn.com/blocks/sidebar)：参考一致组件和响应式布局；沿用当前React/Tailwind/Lucide基础。

## 本批结果

当前已进入统一候选复验。以下为原候选 `08bfe11b` 的探索性真实结果，后继修复不抹去原失败：

| 案例 | 当前原始结果 | 处理 |
| --- | --- | --- |
| S01 | 流程自动通过，独立业务正文失败 | 虚构签到设施/材料承诺；补通用事实来源与Review约束后用同意图复验 |
| M02 | 流程及独立正文通过，一次澄清 | 真实用户答复路径；不表示执行过实际迁移或发布 |
| C01 | Leader intake失败 | 原报告整合节点使用不支持的integrator角色；保留拒绝门禁，修正新配置角色指导 |
| S02 | Leader intake失败 | 观察到write_file尝试失败且无有效决定；补编排Leader不亲自执行的职责提示，不猜测写入目标 |
| M01/S02首次脚本 | 连接后重复新建链接导致定位失败，未创建模型Task | 收敛选择器；归入测试脚本失败，不冒充产品业务失败或模型调用 |

部分早期截图先于页面路由/轮询收敛，文件名不能证明画面状态；后继脚本记录真实tab/status并等待完成，转场截图停用动画。完整最终候选、重复次数、失败及复测和三线缺口在本节续记。

候选 `e3e8ff1753957703f1817f80f2956435652f0115` 的安装清单摘要为 `sha256:39c05c1dc0a409908f01ffd473878b49be9e12848aeafbd190728d7455b999b0`。Qwen CLI 0.23.2 实际报告模型为 `Kimi-K3(openai)`；下列均为隔离安装/数据根、真实浏览器操作和真实模型，失败不从样本中删除：

| 案例 | 用时 | 流程与业务结果 | 证据与后继处理 |
| --- | --- | --- | --- |
| S01 | 106秒 | 失败，无候选、无Review | 原生公开工具记录显示绝对输出目录抄错，范围拒绝正确；未写出的正文标题也错误。不能把路径修正等同于业务通过 |
| M01 | 339秒 | 失败，无权威候选 | 原生公开工具记录显示109字节JSON已正确写入并读回，总额1800分/4笔；后续处理只有通用失败，具体原因待细分类诊断复验，不能猜测为计算错误 |
| C01 | 374秒 | 流程通过，独立业务审查失败 | 两位作者与整合作者真实执行，10条执行记录；方案只明确保存路径/摘要，却承诺从本地数据重建全文索引，缺少内容恢复来源。补通用依赖及恢复可行性审查，不能以字段齐全代替方案可行 |
| S02 | 253秒 | 作者和Review通过，后续Leader决定被拒 | 原报告选择等待，但此时无在途执行，误以为Core自动安排Verification；补Leader显式调度独立验收指导，不改写模型动作。HTML中文日期与ISO表示同一天，修正验收器误拒 |

细粒度执行诊断只公开固定类别及来源，不公开原异常敏感正文；历史诊断不能冒充最终失败根因。Qwen最近一次响应的用量已从显式扩展获得，不能相加为Task累计Token，零值仍可能是桥接默认值。

`e3e8ff17` 受控浏览器故障证据位于研发运行目录 `experience-fault-acceptance-20260914/.marshal/evidence/browser-fault-chromium-VvfBfC/evidence.json`，覆盖取消、丢失一次操作响应、跨路由后显式同键重放；服务回执及审计绑定一致。该证据使用受控Agent，不计入真实模型业务通过次数。真实200%浏览器缩放证据绑定 `10845c4d`；375/1024/1440像素和浅深主题另有视觉证据，后继页面变化按影响范围复验，不将不同候选统称同一版本全通过。当前重复成功出口尚未满足，真人可用性仍未执行。

候选 `5809eabcccd992e7677b278687447d99e8c7c133` 的安装清单摘要为 `sha256:2b01d8329a60f075a999a21e352c4f3919f95d8a44413971390832b60b24e382`，加入固定采集细因及显式Verification指导。M01两次探索仍失败，不能选取中间成功片段标为通过：

- 第一次31秒在intake结束：3063字节原公开JSON存在括号语法错误，无工具调用，未形成计划。不是业务计算或文件采集问题。
- 第二次92秒：计划、作者正确输出与采集、独立Review accept均完成；后继Leader未返回最终公开文本，尝试用 `write_file` 将verify提案写入自己的 `.qwen/tmp/leader-proposal.json`，请求含不支持字段。权限正确拒绝，任务失败。该例不属于JSON语法错误，不能以格式纠错扩大其工具权限。

这两次不同失败要求分别处理原生工具目录与有限协议纠错，不能只补提示词或盲目重复调用。原M01采集失败在第二次未复现，原根因仍未知。失败验收器后继补公开audit/leader投影留存，避免只保留终态粗码；测试脚本修正中文日期等价、S01分区计数及无依据长度门槛，无法识别合法格式明确待独立语义审查。

同候选 S02 用时267秒、8次执行，完整流程及自动锚点/无外网依赖/宽窄布局检查通过；独立正文与画面审查认为排版可接受，**业务可用性拒收**。4145字节HTML新增姓名、联系方式与“提交报名”表单，却没有接收或保存实现，默认GET提交也不等于登记；“未接网络服务”底注不能抵消主操作承诺。该提交按钮未在原脚本范围实际点击，不能把自动PASS扩大成全部交互可用。后继通用评审需核对每个可见操作所承诺效果的实现与权限，无实现应移除入口或明确不可用/演示，不按特定案例禁词模拟业务验收。

### 2026-09-15：候选 d14a660e 的完整业务复验

候选 `d14a660e74195fea4b67235f52c80f15fc49a085`，安装清单摘要 `sha256:49ec03389330cf3188d7ef48780d5f66f4eb0f592eb1147471c3f621ed9c44f3`，84文件/1,642,697字节。主验收与独立审查从实际冻结 Git HEAD 分别打包/核验一致。前一打包因测试夹具命名未被发行排除规则识别而拒绝，修正为 `.fixture.mjs` 后通过，未放宽运行时库存。

| 案例 | 用时 | 自动流程 | 独立业务结论 |
| --- | --- | --- | --- |
| S01-1 | 118秒 | 通过 | 通过，标题/3步骤/2注意准确，无新增设施或服务承诺 |
| S01-2 | 146秒 | 通过 | 通过；累计两次，尚不满足本批三次出口 |
| M01-1 | 128秒 | 通过 | 通过，精确4笔/1800分，区域口径正确 |
| M01-2 | 114秒 | 通过 | 通过，同候选连续两次，未以跳过失败凑样本 |
| S02-1 | 173秒 | 通过 | **失败**：4227字节HTML仍有无接收/保存路径的可编辑表单与主动提交 |
| C01-1 | 256秒 | 通过 | **失败**：允许覆盖工具库既有记录，却以删除本批条目实现“不破坏既有数据”的回退，缺少前值或版本恢复来源 |

以上真实 Qwen 0.23.2 / `Kimi-K3(openai)` 任务均已正常退出服务。S02 独立浏览器真实填写合成信息并点击后，仅发生当前页面 GET，字段清空，无提交/保存；顶部锚点跳转正常、宽窄排版可读不能抵消该失败。其原 Review 明确以“无action且有示意底注”接受，可见规则已经传入却被模型错误解释，不是简单漏传提示。C01 逐项推演工具库既有记录A被B覆盖再撤销B的反例（候选仍要求用户原笔记只读），证明文件布局和方案字段齐全不等于回退可行。两项失败保留，停止无修正的全链重跑。

验收接缝核对：当前固定检查器只有 `exact-files`，绑定正确计划和文件，检查路径、UTF-8、长度和摘要；不会自动执行自由文本 `acceptance` 中每项业务要求。模型 Review 仍是语义判断，不能与固定程序检查合并宣称“所有业务要求已自动测试”。后继改进明确界面的实际检查范围，并以原失败及最小修正正例建立真实 Reviewer 组件评测；组件评测不计入任务整链成功次数，指引加强也不等于确定性语义证明。

同候选已安装受控补验7/7、零跳过、零模型：实际交付、取消、错误金额拒收、正常停开和同键重放、错误源码/清单身份在启动前拒绝。服务与受控ACP均已清理。Core 格式纠错另经 `68d13724` 受控真实 HTTP/SQLite/Chromium 完整链验证：保留原失败、唯一后继、实际输入与Audit逐字一致、回答/批准/Review/检查/下载；375/1440px截图及键盘展开关闭有证据，不计为真实模型纠错或真实业务返工。

`d14a660e` 的 [Node team CI](https://github.com/chiga0/marshal-harness/actions/runs/34869168741) 在 Linux/macOS、Node 22.22.1/24.15.0 的正式回归、UI构建/测试和同包消费全部通过。本地UI603项及4项浏览器护栏通过，格式纠错/恢复相关59项通过；Pages严格构建19HTML/1313站内链接，27操作/72Schema和原OpenAPI一致。该工程验证不关闭上述业务失败。当前真人可用性未执行，也未将此候选发布为全部验收通过的产品。

### Reviewer 正反例与静态页面自动回归

后继候选 `5bb31cbd84cc39f4ed2edd3f8930a92f8bbb152c` 的清单摘要为 `sha256:334cc2512014b00d757fb9814cbedb88837e4c0e1e555e46fa7b48dc8d373b9d`。Review 加强按状态变化构造反例的审查方法，UI 明确配置检查与业务判断的边界；没有新增任意代码执行、外部权限或假定固定程序能证明全部业务语义。

使用脚本 `76957eeb`、输入组摘要 `sha256:9bf5e09b02323a15bed1a48f529924a3972ca1e13738a2bfea021fa55d7b5b97`，对同包真实受管 Qwen Reviewer 串行评测。原负例完整保留；正例只修候选及其派生引用，不改原需求和批准范围。C01 正例保留批次撤销要求，提供前值、版本、事务、后继顺序及重复撤销语义，没有靠删除目标通过。

| 样本 | 原回执判定 | 用时 | 独立结论 |
| --- | --- | --- | --- |
| S02 原失败 | rework | 95.0秒 | 正确指出默认GET、无信息送达路径及虚假操作承诺 |
| S02 修正版 | accept | 31.7秒 | 静态说明与真实锚点，没有主动信息收集入口 |
| C01 原失败 | rework | 131.7秒 | 构造既有A被B覆盖后无法恢复A的具体反例 |
| C01 修正版 | accept | 79.3秒 | 前值恢复与幂等批次撤销方案自洽，无关键误拒 |

四项原闭合回执、摘要和正常清理均经独立核对，公开活动无工具/权限事件。组件脚本审查先修复了正负标签可能经路径泄露、仅检查裸 `verdict` 的假绿风险；实际执行使用不透明身份及安装包原 Review Port/receipt。C01 原负例报告给出的缩减回退范围建议不自动成为合法修正，仍须遵守已批准目标。四项只证明本组 Reviewer 行为，不是 Core 整链或真实业务重新交付。

S02 后继 E2E 加入实际 DOM 表单关联和可操作性检查：该案例没有脚本或接收端，不能保留主动填写/提交并以底注抵消。真实 Chrome 10项组件回归及原失败HTML复验通过，覆盖外置 `form` 关联、disabled fieldset 首个 legend 例外、inert、只读输入 Enter 仍会默认GET等情况；禁用演示与纯锚点可保留。最初遗漏 readonly 隐式提交由独立浏览器反例发现并修正，未改写原失败记录。此检查仅用于冻结的静态邀请页案例，不外推为任意 HTML 安全或行为证明。

### 候选 5bb31cbd 的整链结果与新增缺陷

该候选使用相同 Qwen 0.23.2 / `Kimi-K3(openai)`，组件四项通过后恢复真实浏览器整链。结果不能与前一候选合并凑连续成功次数：

| 案例 | 用时 | 整链与独立业务结论 |
| --- | --- | --- |
| S01-1 | 152秒 | 完整交付及独立正文通过 |
| S01-2 | 244秒 | **失败**：真实Review指出无依据“签到”安排，作者完成一次修正，配置检查及交付已完成；最终Leader公开JSON中的内嵌引号未转义，严格解析失败，未完成整个Task |
| M01-1 | 98秒 | 完整交付及独立算式通过，4笔/1800分 |
| M01-2 | 128秒 | 完整交付及独立算式通过，同候选连续两次 |
| C01-1 | 213秒 | 流程通过，**独立业务失败**：工具内自建标签及挂接关系只有本地权威表一份，却被回退步骤当成可删除的派生数据；从只读原笔记重新导入无法恢复这些用户创建的值 |
| S02、M02 | 未运行 | 本候选尚无对应整链通过证据；旧候选结果不自动继承 |

S01-2 原Task为 `task-45cfbdfe-c291-41d1-a4bf-343437b6b715`，12次执行、一次真实业务返工；原最终公开输出摘要 `sha256:9d33a259b024bfebecd828de7048e0e92ce0d67680b3892935dd01bce2753688`。独立回放原安装包解析器确认为 `invalid_json`，不是Provider网络失败，也不是输出为空；末条粗诊断与历史投影截断不能改变这一事实。当时格式纠错 used=0，因为保守词法分类不接受被漏转义引号切断的裸文字。后继修正须继续保留编码、重复键、深度、非有限数值与后缀混合错误的拒绝边界，不自动补引号或重写动作。中间真实返工成立，但不将其单独计作C02整链通过。原 `state-failed.png` 与 `failure.png` 采集时页面仍显示执行中/收尾，不能作为失败页视觉通过证据；后继采集必须等待真实页面状态，并记录期望/可见状态及未收敛事实。

C01-1 原Review输入完整包含原需求、三份资料、批准计划及全部候选；上下文缺失不是原因。原Review以“未承诺保留自建标签”为由接受，这是候选未明确声明的豁免，不能由评审自行补充。反例为原笔记A不含标签T，工具内创建T并挂接A，删除工具标签映射后重读A；没有保留的权威表或备份便不能恢复A–T。应区分原始资料、工具内用户创建的权威数据与真正可重建缓存，并核对删除范围和恢复来源。先保留新反例及保留权威来源的正确方案，再评估修正；不能把此前四项Reviewer组件通过扩展为通用语义正确。

该候选的[跨平台工程CI](https://github.com/chiga0/marshal-harness/actions/runs/34871063326)13项通过，独立同包受控消费7项通过；这些结论不关闭上述两项失败。当前工程代码与验收脚本继续集成于同一PR，尚未发布本批。真人产品可用性仍未执行。

### 后继协议、界面与评审组件结果

源码/安装候选 `a0854099b9e245e51cb0d66a5b058452eeeef692`，安装清单摘要 `sha256:83a508f4ea324843800c367423280aa728d0fd708bb77f2e9cb26211646728a4`，84文件/1,649,699字节。

- 漏转义引号只扩展两个完整字符串之间的有界Unicode词段分类，不自动修复文本。完整原输出和中英法同形例进入一次纠错；混合重复键、深度、非有限值、非法字符串和后缀仍拒绝。独立102项相关回归及147项词法负矩阵通过；集成Core/HTTP78项通过，不能与重叠套件相加为不同测试数。
- 收尾受控测试复现原32次Attempt/16次Leader调用上限下的12次执行、7次Leader调用及一次业务返工；原失败保留，唯一后继使计数成为13/8/1，正常完成并冷重开保持。预算不足反例拒绝，不放宽额度。纯测试后继 `6b649778` 不改变该包运行代码。
- 受信原Port解析诊断接入 `protocol/invalid_json` 等固定类别，仅用于观察；原Provider自报controller来源、外来或迟到报告不能伪造根因。原失败截图未收敛问题改为有界等待并保存期望/可见状态及不匹配事实；5项真实Chrome组件回归通过。
- Verifier执行名固定为“配置检查”，原自由文本目标标为计划要求；团队速览优先显示该次失败，不将历史失败冒充当前阻塞。独立实际HTTP/SQLite/受控ACP浏览器链绑定 `dab49acf`，其UI与协议运行代码和本候选一致，仅Review指导有差异。1440/375px、键盘、输入逐字校验和下载通过；本候选UI610项及4浏览器护栏通过，同包独立受控消费7项通过。真人可用性仍未执行。

新标签恢复反例的正例只修改三份候选，保留原Task、批准范围和附件；固定规范化路径映射到持久note_id，保留工具内用户创造数据，仅重建缓存。独立审查先修正正例残留的“内容哈希主键”和“不打开原笔记”矛盾，再冻结v2；负例不改。以下均各样本一次，无隐式重试，不改变用户全局模型、登录或产品默认：

| 组件配置 | 错误方案 | 正确方案 | 结论 |
| --- | --- | --- | --- |
| `5bb31cbd`显式请求已配置`qwen3.8-max` | 42秒；原Port拒收带前言的JSON；公开建议还误写accept | 33秒；原Port同样因前言拒收 | 0/2合法组件通过。实际Provider报告为不透明路由标识，不冒称验证了模型品牌；不能据此切换默认模型 |
| `a0854099`默认配置，实际`Kimi-K3(openai)` | 50秒；合法accept，**语义误收** | 61秒；合法accept，来源分类与恢复检查正确 | 1/2通过；新指导不能关闭误收问题，停止对该缺陷无修正的整链重跑 |

两个模型路径都未证明这项能力达标。不能靠增加措辞、隐藏解释文字、改写verdict或挑选一次成功绕过验收。该语义问题继续开放，简单文档和静态页面等独立受影响链路可以继续补验；不会把它们的成功计为复杂方案验收成功。


### a0854099 简单链路复验与作者交接缺口

同一安装候选继续各执行一次 S01、S02，没有更改冻结输入或重试掩盖失败：

| 案例 | 用时 | 自动流程 | 独立业务验收 |
| --- | --- | --- | --- |
| S01-1 | 136秒 | 通过，8次执行，格式纠错未触发 | **失败**：原写作意图允许普通建议，但批准计划额外禁止任何未提供的建议；作者仍加入交流与静音建议，Review又自行按较宽口径接受，批准范围与验收不一致 |
| S02-1 | 172秒 | 通过，3434字节HTML | **失败**：虚构免费、30席、服务台登记、短信确认及截止时间等既有服务事实；实际没有材料依据，Review未拒收 |

S01 不应归因成所有普通写作建议都有问题；应避免 Leader 擅自增加禁令，并要求执行和评审遵守真实批准范围，不能由 Reviewer 默默豁免。S02 在375/1440px的实际锚点、排版与无主动表单检查通过，但静态页面同样可以做出虚假报名承诺，DOM守卫不能替代事实验收。制品摘要分别为 `sha256:37e71a4e1c0e2bcc9ca5168406bc42fe0887bb57ca45fbc049f5706e2b9a8d70`、`sha256:96b285f75bfcdd3e5a611e40a83987ce5389abd66b5e546be007f1472d70f2eb`；两次服务与浏览器均已清理。当前候选尚无本轮完整业务连续通过门禁，真人可用性未执行。

核对 S02 原 Audit 的实际作者提示6562字节（`sha256:e46475e06e034be673ac016a2b39f74656e149572912daaec536353fcc41ef6e`），事实、操作效果与数据来源指导只出现在 Leader/Review 配置，未直接交给作者；依赖 Leader 转述，存在遗漏或过度收紧的风险；本轮已观察到相应偏差。这是明确输入接线缺口，后继以固定作者指导消除转述依赖，并检查实际 Provider 输入与 Audit 逐字一致。它不解释已经完整收到指导的 C01 Reviewer 误收，也不能凭接线测试关闭业务失败。

### Pi Reviewer 的同组有限组件结果

原 `a0854099` 安装包、同一标签恢复 v2 正反输入组（`sha256:36a9071dfd3bdbbf01ad679ad0b90305de84cc8f385b8b1549647914df753dd5`），显式原生 Pi 0.84.4 / `pai-eas` / `DeepSeek/deepseek-v4-pro`，各执行一次。实际原生状态报告 thinkingLevel=high；未改全局模型或登录。组件仍经原 Provider 与 Review Port，不直接采信裸 verdict。

- 负例62.5秒后 Provider failed，未得到合法评审；具体失败原因未保存，**未知**。清理记录的 owner_stop 不是原始故障诊断，不能推断模型拒绝、语义结论或特定网络问题。
- 正例40.5秒，原 Port 接受合法 accept，正确区分原笔记、用户创造的权威表与可重建索引；独立文本检查通过。

本组1/2，不能据此宣称负例判断能力通过或切换默认模型。未观察到工具、压缩或重试活动，不代表原生内部网络重试已被禁止；两个执行均已清理。后继脚本只为未来保留白名单安全失败码，不补造这次缺失原因，也不为补日志重跑模型。组件结论不能计入真实团队整链成功次数。


### 固定作者指导的实施与首轮复验

[ADR0105](../adr/0105-fixed-author-instructions.md) 的实现以 `1eccece1a2168dc5b9dafbb640d1fa286b15d375` 打包，清单 `sha256:a9adf9f4c61696031f04aaba39c0c001778fb57d3264513b62a9ab7ec25514ea`，84文件/1,652,100字节。独立冻结实现回归50项、实际HTTP4项通过：旧提示逐字不变、原业务身份和权限不变、参数闭合、失败准备封窗、冷故障恢复；新策略指导或源码指纹变化在claim前拒绝，generation与marker不变。原生受控ACP实际收到的作者输入与Audit完整下载逐字一致。原包安装消费7项通过，零跳过、零模型。套件有重叠，不相加为独立测试总数。

| 同候选真实案例 | 用时 | 自动流程 | 独立业务验收 |
| --- | --- | --- | --- |
| S01-1 | 86秒 | 通过 | 有限通过：批准范围允许合理创作，标题/步骤/注意/时间地点及原意一致；“现场安排/分享环节”的既定流程语气记录为非阻塞措辞问题，不宣称所有指导严格服从 |
| S02-1 | 239秒 | 通过 | **失败**：精确日程、15:50合影结束和当天直接到场方式仍被写成既定安排，未标为建议或待确认；原材料未提供这些事实 |
| C01-1 | 484秒 | 通过，14次执行，发生一次真实Review返工 | **失败**：标签恢复问题已被有效发现并修正，但两步导入失败重跑的提交/跳过规则仍不完整 |

S01 实际作者输入6383字节，摘要 `sha256:e246ba3070ebfe4982a1be2d57367e54a7d1f254833c916a670f30625ffcb222`；S02实际作者输入8663字节，摘要 `sha256:825228825715b8ae7e1779f249ec17d7420c28715767562b475954499e514728`，两者均确认固定指导已直接送达。S02不再有假表单、虚构名额或短信确认，但原Review仍将日程泛称创作、直接到场认作无伪收集而accept。因此输入接线修复成立，事实与创作边界的执行及评审能力仍未达标；停止无实质修正的重跑，不再把它归因于提示漏传。

S02原HTML摘要 `sha256:c472eff99d4b3f37c58590742b3f4cd2332a5e1d16bf7e953abf11c7da9440a6`；375/1440px实际浏览器消费、锚点点击及无外网/表单检查通过，仅限该交互与布局范围。任务服务、消费HTTP和浏览器均退出。真人可用性仍未执行，旧候选成功次数不并入本候选。

C01 最终5599字节纯JSON，文件摘要 `sha256:8651be68d5d22c00828da28da4b707c24b6da037804168ecf22d8b57ccd3462d`。第一次Review确实指出工具内新增标签不能从原笔记恢复；修复稿明确仅提取原笔记标签、不提供工具新增入口，design/acceptance/rollback同步。原需求未要求新增标签，这次是有效范围内修复，不能将旧T反例直接套用。

本次剩余P1是方案承诺“先记录导入清单、后建索引；任一步失败重跑等价”，同时规定同一标识识别为已导入即跳过，却未定义pending/committed、原子提交或跳过前索引完备判定。清单记录A成功而索引未完成时，按唯一写出的跳过规则重跑可能仍缺A；若实现选择续建，需要补方案没有说明的关键规则。因此判定完整恢复设计缺失，不冒称已经执行候选软件或证明发生数据丢失。第二次Review复述该保证而未验证失败窗口。同路径内容变化后路径+hash新ID的旧记录处置未定义另记P2，因原需求未明确增量更新，不以此单独判阻塞。自然返工和整链收尾成立，完整业务验收仍失败。

### 评审原文的概览层级修正

真实S01概览截图显示数百字Review summary夹杂digest占满主区域。UI候选 `2846a631b1afa237793deb7f6914b2eeef2723d3` 将长理由限制为最多160个Unicode码点的原文节选；含技术引用时只提供展开入口，完整原文逐字保留。评审结论与发现数量明确显示，发现与修改建议保持可见；原绑定读取、原报告和Core不改。新包清单 `sha256:eea14049f01e5a6a752f5ee32bfbbea32a5cb383c3fbeb647f017e2aa95adfda`，84文件/1,653,016字节。

定向9项、集成UI612项及4项浏览器护栏通过，构建通过。独立代码审查未发现绑定或原文丢失问题；375/1440px的hash/长文/短文六组真实Chrome组件检查通过，包括键盘开关及行动可见性。该六组使用受控Reader/Transport组件夹具，不冒充实际HTTP Task或真人验收；实际服务补验另记。源码和UI候选与前一业务安装候选分别绑定，不能将视觉修正称为S02语义修复。


有限实际HTTP补验已完成：正常重开原已完成S01数据根，保留原 `1eccece1` 后端和配置，仅加载 `2846a631` 静态UI；先核验两包后端/配置逐字相同，仅UI index/JS变化。375/1440px长hash摘要默认折叠、Enter展开、Space关闭、原文逐字及完整技术JSON检查通过，折叠态无横溢，展开长hash由实际截图确认换行。未触发模型，原事件125→125，服务exit0、浏览器已关闭。明确为旧后端与新UI的有限组合验收，不能冒称新候选完整同包E2E；真实报告findings=[]，非空行动项只有组件/单元证据，实际HTTP未覆盖。此补验不关闭业务失败或真人可用性缺口。

## 批准前约定与逐项评审的后继候选

本次后继采用 [ADR0106](../adr/0106-bound-review-assessments.md)：在批准前展示原业务条目和固定政策，评审逐项关联原文、来源、状态与必要的反例推演；证据由原 Review Port 回执绑定，并与决定一同持久化。原作者返工读取同一 v2 证据。文件配置检查仍只证明其声明的文件属性，文本判断不代表实际业务效果已验证。

最新统一候选为 `4f68bd27540dacb595f1dc3b0acccaa3115f0b13`，包清单 `sha256:d6b2cf8470bdf6d0f75b0ed83275167adaea00cb83800be2b339df1f48e1d749`，86 文件、1736657 字节。该包同时包含 UI、Core 和 OpenAPI；运行中的测试脚本单独记录摘要，不重新解释旧任务的批准。此前原业务失败记录保持不变。

| 验收线 | 当前证据 | 结论与剩余工作 |
| --- | --- | --- |
| 功能与可靠性：合同/Core | shared `f8aef413`、Core `14592707` 经独立审查；独立两组不重叠回归共 87 项，含原回执、四处新增真实 SIGKILL、配置漂移、返工和冷开 | 受控范围通过；不代表真实模型语义通过 |
| 功能与可靠性：返工 | `b434c38f` 与 shared 经独立 46 项测试及代码审查；坏 v2 不降级，原 v1 行为保留 | 受控范围通过 |
| 功能与可靠性：安装与客户端 | 发行清单排序问题已修，12 项同包/冷导入等测试通过；整合 UI 643 项及 4 项浏览器守卫通过；新增 wire 与 API 22 项通过 | 最新同包实际 HTTP/浏览器链通过下述有限范围，不能替代真实模型业务验收 |
| 视觉与交互 | UI 后继 12 组 Chrome Reader 组件检查覆盖 375/1440、计划、未知、反例、旧格式、空与空白来源；77 项定向测试通过 | 组件及最新同包 HTTP 场景通过；真实用户可用性另验 |
| 业务结果 | 四组最小正反候选从原 Audit 独立重建，摘要 `94b9fc42656d67430ebafad80b52ee5ad692e25f1be3e3625a34980d2d854510` 一致 | 原180秒窗口四例全部超时；延长窗口仅 S02 负例已返回合法返工，尚未证明完整区分能力或从零 E2E |
| 产品可用性 | 无未参与开发的目标用户测试 | NOT_RUN，不以 Agent 操作代替 |

交叉审查已拦截并修复三类缺陷：空来源无法形成合法负评审、UI 允许损坏的状态/finding 组合、固定政策正文被自算摘要替换。修正后的验收仍须按本表分别判定；代码集成不关闭原 S02/C01 业务失败，也不表示发布放行。

前一统一包 `d42eb0be`（清单 `sha256:e688b6637b6258d7f0e29c24dc57558b15a1727e3d8abe4d55ba67edeeee4e19`，86 文件、1735233 字节）的实际 HTTP 验收两次失败：合法原 Provider Review 已完成并清理，但 ExecutionController 构造新 outcome 后丢失以旧 result 身份关联的 sidecar。它没有绕过门禁，却使完整交付不可用。修复 `42ce94f3` 改为原 opaque receipt 绑定并保留原 Port 全部身份校验；独立 35 项回归通过，新增真实 HTTP 双任务、不同 DAG、v2 制品及正常重开回归通过（37.2 秒）。修后包与最新 UI 包均已独立浏览器复测通过；前述直接 Core 受控用例不能撤销这次实际接线失败。


实际服务与视觉后验：`42ce94f3` 清单 `sha256:e9c1db46f83f636b2d72893736645dfd18d1b652f35d754af47ad8f8c57a7f3a` 的 HTTP→批准→执行→v2证据→页面展示通过；继而 `4f68bd27` 同包重新完成该链，覆盖375/1440、固定政策短标题及展开完整原文/理由、键盘、刷新重连、损坏 v2 拒绝且不降级、恢复正确响应。5条计数与原制品一致，scope项展开原文/理由逐字核对，完整v2绑定通过，包前后校验一致，浏览器关闭、服务退出0。证据为独立 UI worktree 下 `assessment-installed-http-XMrjyo` 和 `assessment-installed-http-7Xc7hX` 的 evidence.json、independent-review.json 与截图。此为受控 Provider 的真实服务验证，模型调用0，不冒称真人可用性或原生模型业务成功。

真实模型测量使用修复后 `42ce94f3` 同包、原生 Qwen 0.23.2、其默认报告模型 `Kimi-K3(openai)`，未修改全局配置。四例均为从冻结材料生成的 **NEW_SYNTHETIC_COMPONENT_INPUT**，不是原 Task 重新批准；标签不进入模型输入。180秒窗口结果：S02负例180311ms、正例180304ms、C01负例180306ms、正例180302ms，全部 `component_deadline`、公开正文0字节、无合法报告/逐项证据、清理确认。组件成功0/4，语义4/4无法判定；最终 cancelled/provider_stopped 是主动期限清理的结果，不能冒充初始故障原因。只观察到 starting/waiting/thinking，原生内部网络重试未知。

随后单次短请求在约22秒返回精确JSON，证明当时短请求可响应。针对同一 S02 负例，保留原输入摘要 `sha256:45e2d941787a72bf34f14db57e6f071636d0b7bef0d05e6220b5ce8b9edaeaae`，另作一次600秒延长窗口诊断：373316ms返回16999字节合法逐项报告，verdict=rework，指出未确认日程及参与方式冒充既定安排；原回执/证据合法、清理确认。原180秒失败保持不变，单条延长测量不证明其余三例、延迟稳定性或最新 UI 候选的整链业务能力。运行证据分别留存于集成 worktree 的 `.marshal/evidence/assessment-42ce94f3-1/`、`qwen-short-42ce94f3-1/`、`assessment-long-42ce94f3-1/`；独立语义复核与其余案例继续推进。


长诊断的独立语义复核：S02负例的精确日程、结束时间与直接到场方式缺少确认依据，拒收成立；但逐项结论仍有误收：对“制作中不运行/部署”，报告仅引用要求和计划、以未见日志判pass，没有充分执行事实。且“共读交流必然预设指定书目”的推断不充分，合理创作仍须用正例防止误拒。因此本次不是12条语义全部通过。默认900秒是整个Task预算，Review继承剩余期限；373秒评审会占用后续返工/交付时间，组件600秒新窗口不能证明真实整链能在原任务预算内完成。
