# v1.0.0-rc.prerelease 历史安装资产验证摘要

> **不可执行的历史证据**：本页只记录 2026-08-27 旧实验 tag `v1.0.0-rc.prerelease` 的本地构建结果。它不是 `v1.0.0-rc1` candidate、安装说明、发布 runbook 或当前缺口清单；不得据此创建/推送 tag、触发发布、安装资产或声明 RC1 可用。

生成日期：2026-08-27

构建机器：darwin/arm64（macOS）

Go 版本：go1.27.0

## 历史资产清单

| 平台 | 文件 | SHA256 |
| --- | --- | --- |
| darwin/amd64 | `marshal_v1.0.0-rc.prerelease_darwin_amd64` | `8f342b0dead16cae4d080f02a843b7cdef59dc088f74a31309cc7d81d7b87658` |
| darwin/arm64 | `marshal_v1.0.0-rc.prerelease_darwin_arm64` | `f01216d561ed482537285c8b50f33739bdb6bf58a2aecda8e4c6e8b0fe404f81` |
| linux/amd64 | `marshal_v1.0.0-rc.prerelease_linux_amd64` | `9121a15d9c4e6a436bb262497dad31ae9a602c4cdbc010dfceb7bd42a5fbc52f` |
| linux/arm64 | `marshal_v1.0.0-rc.prerelease_linux_arm64` | `ebdaf0323bd6a7a682507a94e854885dcff4a099744fca9f1d4142c9ea81ec76` |

当时仅证明 `make dist VERSION=v1.0.0-rc.prerelease` 能生成四平台文件、生成 `SHA256SUMS`，且本机 `shasum -a 256` 对 Darwin arm64 文件通过。历史 `marshal version` 为 `marshal v1.0.0-rc.prerelease (unknown, go1.27.0, darwin/arm64)`。

## 为什么不能迁移为 RC1

1. `commit=unknown`，没有冻结 exact sourceHead、build date、toolchain/profile与 current canonical object。
2. 旧四平台集合不是 [ADR 0068](adr/0068-mac-first-cli-only-lifecycle-preview-rc1.md) 的 RC1 支持面；RC1 只支持 unsigned Darwin arm64 CLI-only `darwin-local-dogfood` preview，Linux/server/其它架构不能成为其发布证据。
3. 旧资产没有 S1′→S2′→Attach/rebind→terminalization 的 fixed CLI authority 链，没有真实 Pi + 独立 ReviewDecision `ACCEPTED`，也没有同一最终 bytes 的 host viability、canary、安装与恢复闭合。
4. 当时记录的 lease、ResultIngress、binding 与真实 Pi 缺口是历史快照；其中部分 component 后来已演进，不能继续作为当前权威状态。当前状态只以 [v1.0 Release Readiness 判定表](v1-release-readiness.md) 为准。
5. 旧 lightweight `git tag v1.0.0-rc1`、`git push` 后自动发布和旧 version 输出期待均已撤销。ADR0068要求的RC1 carrier、canary receipt、tag publish与installer guard仍未实现；在这些后续切片落地并经审查前，不得创建tag、不得发布，也不得把本地candidate称为可用RC1。

## 当前已实现的单资产构建合同

`make dist-rc1` 是ADR0068 build-once路径的第一个本地构建切片，不是发布入口。它与原有`make dist`四平台合同分离，并只接受以下精确输入：

- `VERSION=1.0.0-rc1`；
- 40位小写Git commit `COMMIT`；
- UTC秒级`BUILD_DATE`；
- `go.mod` 锁定的精确Go toolchain。

`dist-rc1` 的 `DIST_DIR` 必须是尚不存在的新目录；命令拒绝覆盖、清空或复用已有目录，避免把第二次构建伪装成同一candidate。普通stable `dist`继续保持清空并重建已有`DIST_DIR`的原语义。

输出目录必须且只能包含：

1. `marshal_1.0.0-rc1_darwin_arm64`，其`selfProfile=darwin-local-dogfood`；
2. exact 8-line `RELEASE-MANIFEST`；
3. exact 2-line `SHA256SUMS`。

`verify-rc1-dist`要求调用方同时提供expected `sourceHead`、`buildDate`、`goVersion`、OS、architecture与profile，manifest不能用自身字段充当外部期望值。校验器不执行candidate：固定Go工具的`version -m`证明`goVersion`、`cmd/marshal`、`exe/gc`、`CGO_ENABLED=0`、`GOOS=darwin`、`GOARCH=arm64`、`-trimpath=true`、无VCS settings；有界只读Mach-O解析器再读取实际`buildinfo.version/commit/buildDate/selfProfile`符号并逐项比对。为此RC1保留符号表但继续去除DWARF；缺符号、shell fixture、错误Mach-O header或真实binary identity漂移均fail closed，candidate本身不会被执行。

`SHA256SUMS`只接受64位小写hex、两个逐字ASCII空格和固定文件名；uppercase、tab、单空格、前后空白、重复或乱序均拒绝。任何Linux、amd64、stable、其它RC、符号链接或额外文件也继续拒绝。

该合同只冻结单个candidate的本地bytes和构建元数据，不产生canary、ReviewDecision、`ACCEPTED`、carrier、tag或publication authority。后续workflow必须消费这一对象，而不是在tag job中重建一份“等价”binary。

## 当前迁移入口

- RC1 范围、partial supersession、same-bytes 与 negative claims：[ADR 0068](adr/0068-mac-first-cli-only-lifecycle-preview-rc1.md)。
- 当前成熟度、剩余顺序与发布阻塞：[v1.0 Release Readiness 判定表](v1-release-readiness.md)。
- 当前用户安装行为与“RC1尚不可安装”声明：[安装、升级、回滚与卸载](install-and-upgrade.md)。

本页只保留旧 bytes 与摘要以支持审计追溯，不授权重新分发、promotion、后补签名、后补 receipt 或 stable 晋升。
