# v1.0.0 受保护 stable 候选验收 dossier（2026-09-10）

> 执行附注（同日收盘）：`v1.0.0` 已按 §6 发布，页面 [marshal v1.0.0](https://github.com/chiga0/marshal-harness/releases/tag/v1.0.0)、annotated tag `b9b7c6b7e6a398c0915bb38476cc5920d2c713d7` → `fc2cdc92`，签名 `timestamp:1789017236`、两项验签公式通过。后文就绪时的表述保留当时的记录。

本 dossier 汇总 `v1.0.0` 首次受保护 stable 发行的候选与证据，供放行复核。它不以表述修复任何既有缺口；稳定候选是否放行取决于签名过程与受保护发行的实际完成。当前状态见 [Roadmap 当前表](roadmap-status.md#业务交付当前表)，签名口径见 [ADR 0096](adr/0096-node-stable-asset-signing-minisign.md)。

## 1. 候选身份（三级互证）

| 层 | 值 |
| --- | --- |
| sourceHead / tag | `fc2cdc9298c3e1aaf47615373bb24e6d80e4c719`（受保护 annotated tag `v1.0.0` 在放行时指向同一 SHA） |
| CI | [run 34425993716](https://github.com/chiga0/marshal-harness/actions/runs/34425993716)，五 job 全部成功（双平台回归、单次打包、双平台同包消费） |
| artifact | `10132869579`（node-candidate），package 60 文件/896368 B，Node 24.15.0 |
| 包 manifestDigest | `sha256:828b3ada02fd7a4f857ad527a13ac31664ed56a8d01a9bc5996170c96fa4955d` |
| 运输 ZIP SHA-256 | `d46277e0c00acb4c78ae6200cebb48ae74dab52af8d1bc4a192937408ef5a766` |

三级身份互不替代：tag 不证明包字节，包 manifest 不证明运输，任何一方漂移即拒绝放行。

## 2. 同字节跨环境证明

- 本机 pack `--source` 主线 checkout 从同一 SHA 产 `manifestDigest=sha256:828b3ada…`（60 文件/896368B）。
- CI Freeze job 产 artifact 10132869579；下载恢复后 manifest 与本地一致，`restore-carrier` 通过。
- Linux 实机（Alinux3 x86_64，固定 Node 24.15.0）对同一 SHA 经 `git worktree` 出检的 pack 同样产 `manifestDigest=sha256:828b3ada…`；`restore-carrier` 通过。
- `fc2cdc92` codeload 包两端 SHA-256=`d90bbd94144c05d426f5af469c56db138e99d14e87c099b5cb7a4539c5748dc7`（同一公开来源两次独立计算一致）。

## 3. 真实验收证据（按 Roadmap 分组）

- **真实 Pi（v7 全程受管 Leader + 授权发布验证）**：`pi-live-2/evidence.json` `passed=true`；task-4ec346cd 与 task-a6ee818d 各 12 Attempts、rework=0、retry=0、diagnostics 为空，审批前正常重启通过。`piVersion=0.84.4`、`nodeVersion=24.15.0`。
- **真实 Qwen（layout 1）**：`qwen-live/evidence.json` `passed=true`；task-52ad5eb4、38.249s、3 Attempts、retry/rework=0，east 3 笔/75 cents、west 1 笔/50 cents，coldReplay sameTask/sameAudit/sameWorkers=true。
- **真实局部修正（task-local-repair/v1）**：`natural-content-repair-completed`，acceptance passed，`modelErrorInjected=false`，唯一分支 repair 修复执行完成之后 west=独立重算值（46/-1237），east 原样保留（制品 digest `sha256:bd6baaa608b986f0471a1e4aeeaea81c881d53f65257226d7607d95276866c36`）。`restart.duplicateStarts=0`；SQLite readOnly integrity=ok，61 事件 tail digest=`sha256:eeb3db673b5d7a5214a0c3d2cf700d338e9462ac76e37f36e3fb27e985ed266c`。
- **声明支持面恢复（main b5aa5261）**：16 个声明面套件 `tests=99/pass=99/fail=0/EXIT=0`，涵盖取消、崩溃/SIGKILL 冷回放、SQLite 页数上限 FULL、control-commit/custody/leader recovery、managed-diagnostic、installed recovery、git-live 冷开零重复。瞬时扰动均隔离复跑证明，历史失败原样保留。
- **Linux 实机**：固定 Node 24.15.0 回归 `715 通过 + 4 平台条件跳过 + 12 失败`，失败全部是 `/usr/bin/git` 写死入 fixture 的执行用例（该机 git 在 miniconda、sudo 不可得），按照同一 commit 由 ubuntu-latest CI 承接；安装后生产报告配置完整生命周期 14.7s 通过。
- **长期运行样本**：200/200 波 exit 0（首组，约 80 分钟）零 capacity leak，日志 SHA-256=`385cff19eeaea47bdf8ec55abb61102e9a005f97e07384381ce8b42daf4bf35d`；随后在案的 ≤12h 连续序列写在此 dossier 提交时仍执行，最终终止由当场记录替代。该项只是长运行样本，不等于多日连续稳定——不在稳定发布决议中被解释为稳定性等价。

## 4. v1-release-readiness 对照审计（Node Agent-Team profile, 2026-09-10）

| 旧时检查 | 当前判断 | 证据/说法 |
| --- | --- | --- |
| B1 完整单任务服务（fixed-server T2、取消/超时/查询/协调） | **按当前 profile 关闭**：B1 已由可信单用户本机 PoC 独立复核通过；旧 Go fixed-server T2 不随本 dossier 重启（旧产品线、按旧 proposal 作用域，不以当前 profile 补齐或回标） | Roadmap 当前表 B1 行 |
| B2 受限团队 | **PASSED** | `fc2cdc92` 全证据链 |
| 唯一权威与故障恢复 | **PASSED** | 99/99 声明面；SIGKILL/EFBIG/leader interruption/同路径 restart/同源 response-loss |
| B3 长期运行 | **样本成立（当前最长序列）** | 上§3（200/200 + ≤12h 在执行） |
| macOS 与 Linux 支持面 | **Linux 实机部署与完整链验收通过；macOS 链由每次 macOS CI 与本机真实整轮承接** | 上§3 |
| #212 managed signing/notarization | **旧 Go Mach-O 线外范畴**：Node 为 JS 文本资产，企业公证/notarized 不适用于该类资产；stable 身份域由 ADR 0096 minisign 提供，不在本类资产上另立机器公证平台 | [ADR 0096](adr/0096-node-stable-asset-signing-minisign.md) |
| 发布权限与产物（required CI / 独立 Decision / current receipt/carrier / 同资产受保护 stable 发布） | **材料就绪**；tag、签名、release 属本 dossier 的后项执行行为 | 上§1/§2/§6 |

以上项不以“进行中”隐合免除；执行完成后再行更新为产出口径。

## 5. ENOSPC 处置（按既有 EFBIG 证据，不新增 OS 层推导）

维护者决议按既有证据处理：既有 SQLite 页数上限 FULL（实测页数 38 的 `SQLITE_FULL`）与 storage-write-failure 的受控 I/O 失败有界回滚证据成立；真实 OS 盘满不在本轮执行（本机无 root，无法安全地提供独立小容量 fs，且不对共享宿主的盘做破坏性填充）。ENOSPC 作为既有 EFBIG 证据的边界项如实留档，不作为 stable 声明的一部分，也不据此推导后续等价。

## 6. 放行前执行清单（状态就绪，执行项由决策者启动）

1. ADR 0096 独立 review 关闭并合入（发布权限变更必经 ADR）。
2. minisign 公钥列入仓库（`docs/RELEASE-PUBLIC-KEY.minisign.txt`，本文件另按选定位置建），私钥维护者持有不入仓。
3. 受保护 annotated tag `v1.0.0` → `fc2cdc9298c3e1aaf47615373bb24e6d80e4c719`，推送至远端。
4. minisign -S 签名：manifest 摘要行与运输 ZIP 行（输出清单 `SHA256SUMS` 与 `SHA256SUMS.minisig`）；用 ADR 0096 §1 三级互证公式公开比对。
5. `gh release create v1.0.0`：附 `SHA256SUMS` + `SHA256SUMS.minisig` + 公钥原文 + 本 dossier；notes 引用 Roadmap 当前表,受保护仅主流路径。

以上均为确定性小步操作，不代替既有门禁；发行成功校验 = 三级互证全绿 + 以下载外部新装的验签公式通过。

## 7. 收窄不说（不随放行消灭）

- `usage=null`（模型用量）仍为未知，不改写。
- `trueProcessOverlapProven=false` 仍为当前真值（本候选轮次未证明原进程交叠）；文本耗时/HTTP 观察不冒充进程交叠。
- 12 个 `/usr/bin/git` 写死用例由 ubuntu CI 承接；该边界说明不改变：在部署环境不存在 `/usr/bin/git` 时，产品运行依赖配置项提供既有 git 可执行路径。
- `production=false` / `ordinaryUser=true` 口径维持；本 dossier 不授予生产授权，也不是社区公证或替代签名。
- 企业级签名/notarized/CD 不属于本 dossier 范围。
