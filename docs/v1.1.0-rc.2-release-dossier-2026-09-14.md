# v1.1.0-rc.2 通用文件团队预发布记录

状态：**已签名预发布，GitHub/OSS 分发及公开安装升级通过**。GitHub 发布时间2026-09-14T05:59:08Z，见 [v1.1.0-rc.2](https://github.com/chiga0/marshal-harness/releases/tag/v1.1.0-rc.2)。发行锁定 `2fde5038c7238894aefd11e802cbe9b8d16f07c6` 的 CI 原包，固定公钥验签通过；public prerelease、非 latest，既有 stable `v1.0.2` 与 [RC.1 预发布事实](v1.1.0-rc.1-release-dossier-2026-09-14.md)不变。公开安装包未新建模型 Task；此前真实 Qwen 与浏览器证据保持各自原身份和限定范围。

## 最终候选身份与签名前接纳

| 项 | 精确值 |
| --- | --- |
| sourceHead | `2fde5038c7238894aefd11e802cbe9b8d16f07c6` |
| CI | [run 34806468018](https://github.com/chiga0/marshal-harness/actions/runs/34806468018)、attempt1、13/13成功 |
| producer job / artifact | `103861139415` / `10333386866` |
| ZIP | `marshal-node-candidate-2fde5038.zip`，1543253字节 |
| ZIP SHA-256 | `0a8b114a2210a4953ef2f25b48aa174efb5661390b39bec7f8730c8bbc1d70c6` |
| manifestDigest | `sha256:a438b98f25455395b42b5c208978e487aae4347d9e5c78a8bc6c5e6283b2b285` |
| distribution.mjs SHA-256 | `4a18afefa02ae5c7b83b8b1036c8c12f2744c3a272422e5e4f5325ac8b3f390d` |
| manifest 库存 | 75文件、1516705字节（含UI）；接纳结果的 `files:72` 计数不替代完整 manifest 库存 |
| 签名前 Decision SHA-256 | `cce3fb32d91d15a67099caf6a44800e489ec1ffc742e8214b9fa3576e8ff45db` |

独立审查复算 stage 原 ZIP、manifest、helper 和 Decision 摘要；Decision 为 `APPROVE_SIGN_PREVIEW_CANDIDATE`，仅允许维护者签署上述精确预发布字节，不执行签名或发行。PR #308 主线 `4a86a5e9` 仅更新发行 pins，不能替换产品 source。后文 `7c97986d` 真实模型与混合构建浏览器证据保留原身份，不冒充最终原包真实模型运行。

最终原包在 Darwin arm64 / Node24.15.0 的两布局接纳2/2 PASS，每布局4次原执行/4 Attempts，冷重开重复启动0、modelCalls0。证据为 `/Users/gawain/.marshal-ui-qa/rc2-ci-2fde5038/result.json`，范围 `same-artifact-candidate-consumption-not-release-approval`。

## 签名与分发

维护者完成签名并验证，trusted comment 为 `Marshal v1.1.0-rc.2; source 2fde5038; decision cce3fb32d91d15a67099caf6a44800e489ec1ffc742e8214b9fa3576e8ff45db`。annotated tag object `4a2c2ed380295ea15fdb8e208251be20fb801bd8` 精确指向上述 source，不指向后继文档/pins主线。

GitHub 已公开8项资产：原 ZIP、manifest、SHA256SUMS、SHA256SUMS.minisig、公钥、Decision、distribution.mjs 与 install-node-preview.sh。OSS 自动 [run 34811596878](https://github.com/chiga0/marshal-harness/actions/runs/34811596878) 已 completed/success，不借用 RC.1 分发回执。

维护者从 [公开 OSS](https://github-releases.oss-cn-hangzhou.aliyuncs.com/marshal-harness/v1.1.0-rc.2/install-node-preview.sh) 下载 preview 安装器，`cmp` 核对与签名 Decision 所绑定的安装器一致，再默认从 OSS 安装成功。独立安装根 `/Users/gawain/.marshal-ui-qa/rc2-public-install.U1VFFe/installed`，75文件/1516705字节、source 与 manifest 匹配上述最终 pins；下载证据目录 `.marshal-download-vb3ctvvm` 保留。

公共安装包再执行 RC.1 → 新 `init --replace-launcher` → `serve --generic`（实际 Qwen 可执行入口、包内 UI）→ `status` 自动连接：ready/UI 均200，旧安装与配置保留，正常停服 exit0/stderr0。证据 `/Users/gawain/.marshal-upgrade-private-nTQP3T/result.json`，scope `public-oss-installed-upgrade`，modelCalls0、无 Task。该结果证明公开分发安装及启动升级，不替代此前真实模型任务，也不证明 ETL/外部发布或真人可用性。

安装器末尾仍有旧 `config.mjs` 启动提示；这不是默认通用团队的必需手写配置。用户按下文新版 `init`/`serve --generic` 启动即可；本次不修改已签名的发行资产。

## 发布说明

- 无显式配置的新服务可通过 `marshal serve` 使用内置 Qwen 通用文件团队；支持记录实际 Agent 可执行入口，不要求用户手写业务配置。
- 支持 `--ui`、`--no-ui`、`--port`、`--data-dir`、`--agent-executable`。启动后记录连接文件并显示真实服务/UI 地址，客户端继续通过本地连接记录连接，不公开 token。
- 显式 `init --replace-launcher` 支持受控升级既有未修改 Marshal 启动器；`serve --generic` 显式选择通用配置。旧配置、旧数据不删除，服务活跃或状态不确定时拒绝替换和配置切换，未就绪不等于已停止。
- 修复 Leader 在交付已经完成后仍被示例引导重复 `deliver` 的问题：相应上下文明确要求 `conclude`，不绕过 Core 的交付和证据门禁。

本范围是可信单用户的通用文件交付：Leader 规划、两个互补作者、独立 Review、固定程序读取实际文件核验并收集交付物。**不是 ETL、生产发布、补数据或外部效果验收能力的默认启用**；登录 Qwen 不自动授予外部业务发布权限。设计边界见 [ADR0100](adr/0100-generic-team-default-and-agent-entry.md)。

## 实机结果及失败保留

两次使用真实本机 Qwen `0.23.2`，入口 `/Users/gawain/.local/bin/qwen`，Node `24.15.0`；原始通用配置，无模拟 Provider。目标一致：两位作者分别生成加法与乘法中文 Markdown 小文，独立审查与实际文件验证后保留两份成果，无外部业务操作。

| 验证 | 结论与证据范围 |
| --- | --- |
| 首轮 `7f253964` | Task `task-ac29b7ec-9ddd-427f-80d7-ab391da8384d`。作者、独立审查与固定核验已完成，但 Leader 重复同一 `deliver`，未正常收口；20 Attempts 中16次为 Leader 调用。通过 HTTP 安全取消，最终 `cancelled`，正常停服；失败与成果保留，不能记为通过。 |
| 修后 `7c97986d` | 新独立根、新 Task `task-8b5fbff7-9e6c-4b9b-9259-d181946ebd1c`，不恢复或覆盖首轮。152699ms，`completed`/revision45；9 Attempts（5 Leader、2 author、1 reviewer、1 verifier），retry0、rework0。行动为 plan → review → verify → deliver → conclude，无重复 deliver。 |
| 成果核对 | Review `accept`，acceptance `passed`，publication `null`；下载两份真实文件，逐份核对 UTF-8 字节数与 SHA-256，并确认 `2+3=5`、`3×4=12`。交付摘要 `sha256:6e8e30f7a4e9fec883ea208b6393be3c1c7ea3e4ad4ebe1b26a9ccbaf2c62fe6`。 |
| 清理与指标边界 | 修后服务 exit0、signal null、stderr0，已停服供原根 UI 只读验收。模型 token/cost 为 unavailable；audit firstReview 计数0/0，不据此声称首审通过率。明确的 Leader review verdict 与该统计字段分开记录。 |

私有证据：`/Users/gawain/.marshal-qwen-generic-kuEPjg/result.json`（首轮）与 `/Users/gawain/.marshal-qwen-generic-rKXcHy/result.json`、`delivery.json`（后继）。连接凭据、原始运行数据不进入发行包。后继设置了同一 `deliver` 在第二个独立 Decision 再现即取消的验收止损规则；本次未触发。此源码运行证明不等于公开发行包安装验证。

## 三条验收线

| 线 | 当前结论 | 仍需完成 |
| --- | --- | --- |
| 功能与可靠性 | 真实通用文件团队、最终 CI 原包接纳及公开安装升级范围 PASS；完整范围 PARTIAL | 用户部署后的真实任务待反馈；原包无模型消费不替代真实模型或业务范围。 |
| 视觉与交互 | SCOPED_CHROME_TECHNICAL_PASS；完整范围 PARTIAL | 同一已完成 Task 的原数据根已完成下述 Chrome 核验；未重跑运行中动态刷新、缩放全矩阵，不代表完整 UI 验收。 |
| 产品可用性 | NOT_RUN | 未参与开发的真实目标用户无指导测试，不能用 Agent 模拟或 reviewer 代替。 |

本次预发布不关闭 UI stable 三线要求，不外推 Safari 未测范围或任意业务的生产可用性。验收规范见 [UI 三条独立验收线](ui-1/acceptance.md#三条独立验收线)。

### Chrome 原成果只读验收

正常重开后继实机 Task 原数据根，未重复模型执行（newModelExecutions=0）。核对真实任务列表与详情、两个作者到 verifier 的3节点/2依赖 DAG、节点至 Worker 详情、`qwen-acp` 与 `edit:completed`、终态取消不可用、Esc 关闭抽屉、Review accept、acceptance passed 及 delivery ready。浏览器实际下载1464字节 `deliverables.json`，UI 摘要复核与磁盘文件 SHA-256 均匹配上文交付摘要；退出连接并清空剪贴板。

发现并修复 P2 文案问题：原概览把外部 publication/postverify 为空称为「暂无交付/后验动作」，与成功文件交付混淆。`557ac334` 改为「发布与后验」及「无发布/后验动作；文件交付请查看成果页」，同一真实 Task Chrome 复测通过。原 UI 源码 `81105f5e` 与集成 `391b4be6` 字节一致；修后 UI 构建于 `592db98e`，JS SHA-256 `d66c1a65949ae97533ebcbde0713b3a5d35ce2e9c0d6eb171762788960493905`，运行侧仍 `7c97986d`。该组合记录不是尚未冻结的发行包身份。私有结果与截图见 `.marshal/ui-generic-browser-20260914/result.json` 及同目录图像。

### 私有 HOME 升级实测

在 `/Users/gawain/.marshal-upgrade-private-q5Jnzf` 中，以已验证 RC.1 安装实际运行 init 生成旧 launcher，再以 `7c97986d` 本地打包恢复的安装目录执行 `init --replace-launcher`，返回 `replaced:true`；本地设置指向新安装，旧配置文件与旧安装根保留。随后由新 launcher 执行 `serve --generic`、指定真实 Qwen 路径和 UI 目录：默认数据根为该私有 HOME 的 `.marshal-node/generic-team`，`status` 自动连接，`/ready` 与 `/ui/` 均200，正常 SIGTERM 停服 exit0/stderr0。没有创建 Task、模型调用0，未触碰用户真实 HOME 的设置。

结果见该私有目录的 `result.json`。这一次新端为本地源码恢复包，范围明确是 `local-restored-candidate-not-final-CI-release`，与随后原包检查分开。

后续使用最终 CI 原安装包 `/Users/gawain/.marshal-ui-qa/rc2-ci-2fde5038/installed`，在全新私有 HOME `/Users/gawain/.marshal-upgrade-private-Z9Urcx` 复验同一升级链：RC.1 init → RC.2 `init --replace-launcher`（replaced:true）→ `serve --generic` 指定真实 Qwen 与包内 UI → `status` 自动 connected；`/ready`、`/ui/` 均200，默认新数据根正确、旧配置/旧安装保留，正常 SIGTERM exit0/stderr0。无 Task、modelCalls0，证据为该目录 `result.json`，范围 `final-ci-candidate-upgrade`。这已完成最终原包限定升级验证，但未运行公开安装器，不证明签名分发已发生。

## 公开发行后用户升级与验证安排（待执行）

1. 仅在 RC.2 真实发布后使用其专用 preview 安装器，校验固定公钥签名、摘要与 manifest；保留 stable 安装。不要使用 stable 安装命令隐式升级预发布。
2. 记录旧安装入口、设置和数据位置，正常停止自己拥有的服务；不因 `ready` 失败假定服务退出。保留旧安装根以供旧连接和身份检查，不先删除旧 root。升级不自动停止旧服务，也不迁移旧 SQLite。
3. 新包已验证后，以实际绝对安装根替换下例 `NEW`。这是升级启动器的显式操作，不是手写配置：

   ```sh
   node NEW/packages/task-local/main.mjs init --install-root NEW --replace-launcher
   marshal serve --generic --agent-executable /absolute/path/to/qwen --ui NEW/apps/task-web/dist
   ```

   `NEW` 和 Agent 路径必须换成实际绝对路径。原启动器若未知、被修改、为链接、权限不安全或与记录身份不匹配，升级应拒绝，不删除或强行覆盖。历史 Node 路径变化导致无法精确验证也应拒绝；需核对旧入口，不猜测身份。
4. 原显式业务配置默认保留；只有 `--generic` 才切换为内置文件团队，并使用新的默认 `~/.marshal-node/generic-team` 数据根（本次显式 `--data-dir` 除外）。本次建议沿用默认设置目录。若选择独立 `--settings-dir`，每次 init/serve/status 均须传同一目录，客户端也需显式选择对应设置；launcher 不自动记住该参数。不得将切换默认配置描述为旧任务迁移。
5. `marshal serve` 是前台命令，应保持该终端运行，在另一个终端执行 `marshal status`，不要把 status 顺序接在 serve 后等待。核对启动输出的真实 UI 地址及客户端自动连接；完成一条小型文件团队任务，下载核对实际成果。验证停服再启动后能看到原任务，而不是新建 Task 或重新执行模型来冒充恢复。
6. 记录实际发行 source/pins、Node/Agent 版本、升级前后设置与数据保留结果、退出状态。任何失败保留现场和错误码，不重试结果不确定的操作。

上述用户环境任务仍待执行；维护者已完成 RC.2 签名、GitHub/OSS 预发布、公开安装及私有 HOME 限定升级启动核验。用户实机业务结果与完整 UI 三线仍按实际证据验收，不因发行成功自动通过。
