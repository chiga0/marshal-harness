# v1.0.1 Node 22 兼容发行记录

状态：候选验证通过，等待维护者交互签名，尚未发布。新版本不覆盖 v1.0.0。

## 范围与边界

本次为 Node 运行时兼容补丁：按 [ADR0097](adr/0097-node-capability-based-runtime-admission.md) 接纳 Node 主版本至少22，不设上限；启动时校验必要 SQLite 能力。Node22.22.1 缺少 SQLite `defensive`，运行时明确告警，固定 SQL、外键、事务、秘密保护和默认未授权交付不变。不修改 HTTP Schema 或持久化格式，不增加 Agent 权限，也不宣称所有未来 Node 版本已经实测。

用户环境的 Node22.22.1 是本次重点验证版本。模型登录及 Qwen 配置继续由用户提供，本次安装不启动模型、不复制凭据。旧 v1.0.0 的真实模型与长期样本属于其原始 sourceHead，不冒充本补丁新增的真实模型验收。

## 精确候选

- sourceHead：`b90d7e7247a690db2af740078c285331569aa496`。
- 本机冻结源码包：61文件、898773字节，manifest `sha256:10c747d2f25dce6c085a736c2ed3e55f19ed9f0517e25a3b8e8561f08f9240f7`；发行时必须与 CI 原始包独立比对，此处不是运输 ZIP 身份。
- 兼容实现：[PR #281](https://github.com/chiga0/marshal-harness/pull/281)，已独立审查，无剩余 P0/P1。
- CI：[Node team 34451556121](https://github.com/chiga0/marshal-harness/actions/runs/34451556121)，attempt1，九项全部通过：四项 Node22.22.1/24.15.0 × Linux/macOS 回归、一次打包及四项同包消费。常规 [CI 34451556132](https://github.com/chiga0/marshal-harness/actions/runs/34451556132) 亦全部通过。
- 原始 artifact：`10142384384`，运输 ZIP 920428字节，SHA256 `a94f53073c96f813a7fbd24edc15a77c32130329a3fbef877d8371e9ec17a2a1`，名称 `marshal-node-candidate-b90d7e72.zip`；manifest 与本机冻结包相同。

## 当前验证记录

- Node22.22.1：关键 runtime/composition/launch 31项、候选准入与实际打包还原19项、外部消费者纯度/负向证据独立3项通过。
- Node24.15.0：独立核心79项、包与还原18项、最终服务能力前置16项通过。
- 本地完整旧进程运行期间发现四项入口/测试接缝问题，均已修复并定向复验；不把该旧进程结果写为最终源全量通过，以本次 exact-head CI 记录为准。
- Node22.22.1 的原始候选准入通过：两种安装 layout 各四次原执行、四个 Attempts，冷回放重复启动0，`modelCalls=0`。这是受控团队验证，不是新的真实模型验收。
- 新安装器10项失败关闭/版本范围测试通过；最终公开发行下载与验签验证待维护者签名发布后完成。

## 发布步骤

取 CI 原始 ZIP，不重新打包；核对 sourceHead、manifest、ZIP，独立验证后使用维护者授权的本地 minisign 签署清单。创建 annotated `v1.0.1`，发布原包与签名材料；新安装器绑定全部精确身份并进行 Node22 实际下载、验签与恢复安装测试。历史包、tag 和签名不变。
