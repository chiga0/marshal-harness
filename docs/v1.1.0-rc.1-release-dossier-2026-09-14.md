# v1.1.0-rc.1 Chrome 优先 UI 预发布记录

状态：**已预发布**（[GitHub v1.1.0-rc.1](https://github.com/chiga0/marshal-harness/releases/tag/v1.1.0-rc.1)，2026-09-14T01:27:51Z）。用户明确批准「先发布，然后实机测试」后，维护者完成签名、GitHub 与 OSS 同字节分发及公开安装核验。Release 为 public prerelease、非 latest；当前 stable 保持 `v1.0.2`，UI-1 仍为 `IN_PROGRESS`。

## 范围与用户预期

本次是可信单用户 Node 服务的 Chrome 优先 UI 预发布，包含任务列表/卡片、任务详情及 DAG/团队、问答与批准、运行控制、制品、审计及设置等已合入能力。API 已发布范围不被撤销；页面中的结果只来自服务事实。预发布供部署后实机体验和反馈，不宣称所有业务可用、全部浏览器支持或完整 UI stable。

三条验收线继续独立记录：功能与可靠性 `PARTIAL`、视觉与交互 `PARTIAL`、真人产品可用性 `NOT_RUN`。Chrome 已测技术范围不再等待 Safari 下载许可才分发；Safari 和真人缺口明确保留。完整历史与具体限定范围见 [UI 验收记录](ui-1/release-validation-2026-09-11.md)，本次安排见 [验收规范](ui-1/acceptance.md#无指导可用性测试)。

## 原包身份

| 项 | 精确值 |
| --- | --- |
| sourceHead | `67317d7b8d08c59b82d7fffc4ad934666c8e3ca8` |
| CI | run `34613104390`、attempt `1`、producer job `103312251209` |
| artifact | `10269822117` |
| ZIP | `marshal-node-candidate-67317d7b.zip`，1519920 字节 |
| ZIP SHA-256 | `07ab43c2f67390c5c84869999380428f65d7218e06421ada93cad9595ce76908` |
| manifestDigest | `sha256:163b19cbfb6dad1ca9cca11d08125ee144e631ecdac859abbea2f5f998ece359` |
| 安装文件 | 70 文件、1495148 字节 |

发行复用该 CI 原始字节，不用后继仅文档 main SHA 替代产品 source，也不重新打包。9月13日恢复同一未过期 CI 资产，原 ZIP 大小及摘要一致，重新消费 2/2 PASS：两布局各4次原执行/4 Attempts、冷恢复重复启动0、modelCalls0。9月11日限定同根升级/回退已核验；旧临时原件丢失与9月13日新消费证据分开记录，不虚构升级重跑。

## 最新浏览器证据

- Chrome 原安装包 E21：真实标签页隐藏，不覆写 DOM visibility。列表前台约5.01秒、隐藏31.00秒、恢复约5.01秒；同一已取消 Task 详情隐藏46.699秒零请求，恢复约2.01秒。仅有限样本，不代表持续负载或任意活动任务的长时间稳定性。
- Safari 原安装包受控 Task `task-646ac3f1-cff9-4008-a9f2-e81e68b2c7f8` 完成创建、回答 `north`、计划批准、双作者、独立 review/验收、明确批准本机报告发布及后验，最终 `completed`。93字节报告摘要 `sha256:6f0187a48bde1dad9fc2938f4212dfe7bb8c04a8f011471815589f78001c829c`，独立本地读取核对 east=10、west=20、region=north；发布启动1次，9次 ACP 执行均清理。
- Safari 浏览器下载仍为 `WAITING_BROWSER_PERMISSION`；本地发布文件核对不冒充浏览器下载消费。上述使用确定性 ACP fixture，不是新一次真实模型或真人可用性验收。
- 原私有 fixture 不兼容 UI 合法无 requirements 输入造成的失败已保留，原 Task 经正式取消确认；修复仅测试 fixture，不混入产品包。失败不被成功覆盖。

私有证据位于仓库忽略目录：`.marshal/ui-e21-authenticated-result-20260913.json`、`.marshal/ui-e21-detail-result-20260913.json`、`.marshal/ui-safari-compatible-20260913/browser-result.json` 与 `local-report-result.json`。不随发行泄露连接 token、用户凭据或原始运行目录。

## 分发与发布后实机验收

维护者固定公钥 minisign 验签与 ZIP/manifest 的 `shasum -c` 均通过；签名 trusted comment 为 `Marshal v1.1.0-rc.1 Chrome-first preview; source 67317d7b`。发行 Decision SHA-256 为 `b2a5f2d65628570640dc77fd906fbffc6f5526e171caba940d2ae5c04aab104d`。annotated tag object `c4d500535fc6e6f27ef7db62293783eb46153dbc` 精确指向上述 source。GitHub 公开8项资产：原 ZIP、manifest、SHA256SUMS、签名、公钥、Decision、distribution.mjs、install-node-preview.sh；预发布未覆盖既有 stable。

OSS [run 34796038321](https://github.com/chiga0/marshal-harness/actions/runs/34796038321) 已 `completed/success`，使用主分支显式 preview 通道。自动 release 事件触发的 [run 34795996441](https://github.com/chiga0/marshal-harness/actions/runs/34795996441) 因 tag 指向的旧 workflow 不含 preview 通道而失败；失败记录保留，后继不改产品包，按已合入主分支通道执行，不能算产品业务失败。

主侧使用 GitHub 公开下载的 preview 安装器默认从 OSS 安装，session30168 exit0。独立安装目录 `/Users/gawain/.marshal-ui-qa/preview-install-20260914.qb61Ow/installed`，Node24.15.0；70文件/1495148字节、source/manifest 与上述 pins 一致，minisign 验签及摘要检查通过。GitHub 与 OSS 的 preview 安装器经 `cmp` 字节一致；latest API 确认仍为 v1.0.2。未启动 Agent/服务、未改 stable launcher/config，不以此安装核验外推业务实机通过。

使用专用 [OSS preview 安装器](https://github-releases.oss-cn-hangzhou.aliyuncs.com/marshal-harness/v1.1.0-rc.1/install-node-preview.sh)，不要用 stable 安装命令请求本预发布版本；应先下载到本地并检查命令，再在独立目录安装，保留既有 stable 与业务数据。

发布后先从公开资产安装，在用户获准的本机/环境完成真实任务、双 Worker 分工、独立检查、取得并核对成果；授权交付仅在明确批准的目标内测试。记录实际浏览器、Provider、候选摘要及问题，不以安装成功替代任务验收。另完成未参与 UI 开发的真实目标用户无指导测试，补 Safari 下载消费。缺陷集中处理；本次预发布不关闭这些出口。
